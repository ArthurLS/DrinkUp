<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mobilesocial.drinkup">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".App_Settings"
            android:label="App Settings"
            android:parentActivityName=".Home" />
        <activity
            android:name=".Credits"
            android:label="Credits"
            android:parentActivityName=".Home"/>

        <activity
            android:name=".Game_Settings"
            android:label="Game Settings"
            android:theme="@style/game_settings_theme" />
        <activity
            android:name=".Game"
            android:label="Game"
            android:parentActivityName=".Home"/>
        <activity android:name=".Game_End" />
    </application>

</manifest>